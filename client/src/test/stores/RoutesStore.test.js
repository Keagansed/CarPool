//import RoutesStore from '../../js/stores/RoutesStore';

describe ('Routes Store test functions', () => {
    // it ('', () => {

    // });
    let data, values;
    it ('initializes variables correctly', () => {
        // values = {
        //     routes : [],
        //     routeSuccess : false
        // };
        //
        // data = {
        //     routes : RoutesStore.routes,
        //     routeSuccess : RoutesStore.routeSuccess
        // };
        //
        // expect(data).toEqual(values);
        expect(true).toBe(true);
    });
    
    // it ('does not retrieve any routes with false token', () => {
    //     values = {
    //         routes : []
    //     };
    //
    //     RoutesStore.getRoutes('123gotoken!');
    //
    //     data = {
    //         routes: RoutesStore.routes
    //     };
    //     expect(data).toEqual(values);
    // });

    // it ('does not create new routes with false token', () => {
    //     RoutesStore.newRoute('123gotoken!', 'here', 'there', {}, '08:00', 'TheTestRoute', true);
    //     expect(RoutesStore.routeSuccess).toEqual(false);
    // });
});